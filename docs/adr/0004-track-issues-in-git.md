# issues/ をバージョン管理対象にする

issues/ の Issue ファイルは issue-logger が生成するエージェント実行用の課題バックログで、別環境・別セッションの AI エージェントが参照して初めて価値が出る。PR #142 ではスクラッチファイルとして .gitignore 対象にしていたが、複数環境での同期と共有を目的にトラッキングへ切り替える。完了済み（DONE/WONTFIX）も「対応済みの記憶」として削除せず残す運用とする。
