---
title: "記事カードにはてなブックマークのコメントを表示する"
status: TODO
created: 2026-06-12T05:10:00+09:00
---

記事カードに表示される「はてなブックマークのコメント」について、現在は最大3件までしか表示されない仕様になっていますが、これを全件表示するように修正してください。

【対象ファイル: `src/client/components/ArticleCard.tsx`】
以下の変更を行ってください：

1. コメントリストの描画部分にある `.slice(0, 3)` を削除し、`article.bookmarks.map(...)` としてすべてのコメントをループして描画するように変更してください。
   - 変更前: `{article.bookmarks.slice(0, 3).map((bookmark) => (`
   - 変更後: `{article.bookmarks.map((bookmark) => (`

2. 「他 X 件のコメント...」と表示している省略用のUIコードブロックを完全に削除してください。
   - 削除対象:
     {article.bookmarks.length > 3 && (
       <li className="text-xs text-muted-foreground">
         他 {article.bookmarks.length - 3} 件のコメント...
       </li>
     )}
